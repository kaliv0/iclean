# from bar import munc, func
from bar import munc
import logging
# from hack import skunk, trunk
# from difflib import *
from pprint import pprint as pp


if __name__ == "__main__":
    print("bar")
    pp({"john": "osp("})
    # urllib.parse.urlencode({"fizz": "buzz"})
    logging.warning("func")
    munc()
